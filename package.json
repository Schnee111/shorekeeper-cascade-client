{
  "name": "shorekeeper-jarvis",
  "module": "server/src/index.ts",
  "type": "module",
  "scripts": {
    "dev:server": "bun run --watch server/src/index.ts",
    "dev:client": "cd client && bun run dev",
    "build:client": "cd client && bun run build",
    "build": "cd client && bun run build"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
